#include "Engine.h"
#include <iostream>

// enum pour l'engine (pas en tant que classe)
enum engine_enum

{
    NetworkEngine = 0,
    GraphicEngine = 1,
    GameEngine = 2,
    SoundEngine = 3,
    PhysicalEngine = 4,
    ScriptEngine = 5,

};
